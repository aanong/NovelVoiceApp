<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.novelvoice.mapper.SysUserRoleMapper">

    <resultMap type="com.app.novelvoice.entity.SysUserRole" id="SysUserRoleResult">
        <result property="userId"     column="user_id"      />
        <result property="roleId"     column="role_id"      />
    </resultMap>

    <delete id="deleteByUserId" parameterType="Long">
        delete from sys_user_role where user_id = #{userId}
    </delete>
    
    <delete id="deleteByRoleId" parameterType="Long">
        delete from sys_user_role where role_id = #{roleId}
    </delete>
    
    <insert id="insert">
        insert into sys_user_role(user_id, role_id) values(#{userId}, #{roleId})
    </insert>

    <insert id="batchInsert">
        insert into sys_user_role(user_id, role_id) values
        <foreach item="item" index="index" collection="userRoles" separator=",">
            (#{item.userId},#{item.roleId})
        </foreach>
    </insert>
    
    <select id="selectRoleIdsByUserId" parameterType="Long" resultType="Long">
        select role_id from sys_user_role where user_id = #{userId}
    </select>

</mapper>
