<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.app.novelvoice.mapper.ChapterMapper">
    <insert id="insert" parameterType="com.app.novelvoice.entity.Chapter" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chapters (novel_id, title, content, chapter_no)
        VALUES (#{novelId}, #{title}, #{content}, #{chapterNo})
    </insert>

    <select id="selectByNovelId" resultType="com.app.novelvoice.entity.Chapter">
        SELECT id, novel_id, title, chapter_no FROM chapters WHERE novel_id = #{novelId} ORDER BY chapter_no ASC
    </select>

    <select id="selectById" resultType="com.app.novelvoice.entity.Chapter">
        SELECT * FROM chapters WHERE id = #{id}
    </select>
</mapper>
